apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: saleor-dashboard-qa
  namespace: argocd
spec:
  project: soh-qa
  source:
    repoURL: 'https://github.com/Simple-Online-Healthcare/forge_k8s_manifests.git'
    path: deployments/apps/soh-dashboard
    targetRevision: deploy-qa
    directory:
      recurse: false
      jsonnet: {}
  destination:
    namespace: saleor-dashboard-qa
    name: forge-qa
  syncPolicy:
    automated:
      prune: true
      allowEmpty: true    
    syncOptions:
      - CreateNamespace=true
