apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: forge-patient-frontend-develop
  namespace: argocd
spec:
  project: sop-develop
  source:
    repoURL: 'https://github.com/Simple-Online-Healthcare/forge_k8s_manifests.git'
    path: deployments/apps/forge_patient_frontend
    targetRevision: deploy-develop
    directory:
      recurse: false
      jsonnet: {}
  destination:
    namespace: forge-patient-frontend-develop
    name: eks-forge-dev-cluster
  syncPolicy:
    automated:
      prune: true
      allowEmpty: true    
    syncOptions:
      - CreateNamespace=true
