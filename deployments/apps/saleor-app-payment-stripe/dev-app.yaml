apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: saleor-payment-qa
  namespace: argocd
spec:
  project: forge-qa
  source:
    repoURL: 'https://github.com/Simple-Online-Healthcare/forge_k8s_manifests.git'
    path: deployments/apps/saleor-app-payment-stripe
    targetRevision: deploy-qa
    directory:
      recurse: false
      jsonnet: {}
  destination:
    namespace: saleor-payment-qa
    name: forge-qa
  syncPolicy:
    automated:
      prune: true
      allowEmpty: true    
    syncOptions:
      - CreateNamespace=true
