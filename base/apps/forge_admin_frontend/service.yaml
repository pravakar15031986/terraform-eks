apiVersion: v1
kind: Service
metadata:
  name: forge-admin-frontend-stage
  labels:
    app: forge-admin-frontend-stage
spec:
  type: ClusterIP
  ports:
  - name: http-admin
    port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: forge-admin-frontend-stage