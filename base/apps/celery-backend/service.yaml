apiVersion: v1
kind: Service
metadata:
  name: saleor-celery-staging
  labels:
    app: saleor-celery-staging
spec:
  type: ClusterIP
  ports:
  - name: http-celery
    port: 80
    protocol: TCP
    targetPort: 8000
  selector:
    app: saleor-celery-staging
