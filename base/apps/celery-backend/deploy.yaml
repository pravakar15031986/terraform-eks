apiVersion: apps/v1
kind: Deployment
metadata:
  name: saleor-celery-staging
  labels:
    app: saleor-celery-staging
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  replicas: 1
  selector:
    matchLabels:
      app: saleor-celery-staging
  template:
    metadata:
      labels:
        app: saleor-celery-staging
    spec:
      containers:
        - name: saleor-celery
          image: 476114148868.dkr.ecr.eu-west-2.amazonaws.com/saleor-celery-staging
          env: 
            - name: ISBEAT
              value: 'false'
          envFrom:
            - configMapRef:
                name: saleor-celery-env-cm
            - secretRef:
                name: saleor-celery-env-sec
          ports:
            - containerPort: 8000
              name: http-celery                  
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "1024Mi"
          command: ['/bin/bash', '-c', 'chmod +x ./scripts/celery.sh; ./scripts/celery.sh;']
