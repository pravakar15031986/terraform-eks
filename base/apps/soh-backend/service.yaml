apiVersion: v1
kind: Service
metadata:
  name: saleor-backend-staging
  labels:
    app: saleor-backend-staging
spec:
  type: ClusterIP
  ports:
  - name: http-saleor
    port: 80
    protocol: TCP
    targetPort: 8000
  selector:
    app: saleor-backend-staging
