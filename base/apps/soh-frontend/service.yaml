apiVersion: v1
kind: Service
metadata:
  name: saleor-frontend-stage
  labels:
    app: saleor-frontend-stage
spec:
  type: ClusterIP
  ports:
  - name: http-saleor
    port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: saleor-frontend-stage