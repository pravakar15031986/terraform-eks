apiVersion: v1
kind: Service
metadata:
  name: assessment-api-qa
  labels:
    app: assessment-api-qa
spec:
  type: ClusterIP
  ports:
  - name: http-assessment
    port: 80
    protocol: TCP
    targetPort: 9000
  selector:
    app: assessment-api-qa
