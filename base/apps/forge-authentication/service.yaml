apiVersion: v1
kind: Service
metadata:
  name: forge-authentication-develop
  labels:
    app: forge-authentication-develop
spec:
  type: ClusterIP
  ports:
  - name: http-forge-authentication
    port: 80
    protocol: TCP
    targetPort: 9000
  selector:
    app: forge-authentication-develop
