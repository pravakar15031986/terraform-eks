apiVersion: v1
kind: Service
metadata:
  name: forge-patient-frontend-develop
  labels:
    app: forge-patient-frontend-develop
spec:
  type: ClusterIP
  ports:
  - name: http-admin
    port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: forge-patient-frontend-develop
